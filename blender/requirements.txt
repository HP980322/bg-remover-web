# BG Remover Blender addon — Python dependencies
#
# These packages are installed AUTOMATICALLY by the addon into Blender's
# bundled Python on first enable (or when missing). You do NOT normally
# need to install anything yourself.
#
# This file exists for reference, and so you can do a manual install if
# you ever need to:
#
#   "<blender>\python\bin\python.exe" -m pip install --user -r requirements.txt
#
# The onnxruntime version is pinned because onnxruntime 1.21+ has a known
# DLL initialization regression on some Windows installs. 1.20.1 is the
# last release tested working everywhere.

onnxruntime==1.20.1
numpy
scipy
Pillow
